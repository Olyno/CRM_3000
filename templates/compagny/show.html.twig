{% extends 'base.html.twig' %}

{% block title %}Compagny{% endblock %}

{% block body %}
    <h1>Compagny</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ compagny.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ compagny.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('home') }}">back to list</a>

    <a href="{{ path('compagny_edit', {'id': compagny.id}) }}">edit</a>

    {{ include('compagny/_delete_form.html.twig') }}
{% endblock %}
